apiVersion: v1
kind: ConfigMap
metadata:
  name: branch-service-config
  namespace: banking
data:
  application.properties: |
    spring.application.name=branch-service
    server.port=8083
    
    # PostgreSQL Configuration
    spring.datasource.url=jdbc:postgresql://postgres-service:5432/branch_db
    spring.datasource.username=postgres
    spring.datasource.password=postgres
    spring.datasource.driver-class-name=org.postgresql.Driver
    
    # JPA Configuration
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
    
    # Kafka Configuration
    spring.kafka.bootstrap-servers=kafka-service:9092
    spring.kafka.consumer.group-id=branch-service
    
    # Disable Eureka
    eureka.client.enabled=false
    eureka.client.register-with-eureka=false
    eureka.client.fetch-registry=false
    spring.cloud.discovery.enabled=false
    
    # Actuator
    management.endpoints.web.exposure.include=health,info
    management.endpoint.health.probes.enabled=true
    management.health.livenessState.enabled=true
    management.health.readinessState.enabled=true

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch-service
  namespace: banking
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branch-service
  template:
    metadata:
      labels:
        app: branch-service
    spec:
      containers:
        - name: branch-service
          image: branch-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: JAVA_OPTS
              value: "-Xmx384m -Xms192m -XX:+UseG1GC -XX:MaxMetaspaceSize=112m -XX:MaxGCPauseMillis=100"
            - name: SPRING_CONFIG_LOCATION
              value: file:/config/application.properties
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 90
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            requests:
              memory: "448Mi"
              cpu: "250m"
            limits:
              memory: "768Mi"
              cpu: "600m"
      volumes:
        - name: config
          configMap:
            name: branch-service-config
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z postgres-service 5432; do echo waiting for postgres; sleep 2; done;']
        - name: wait-for-kafka
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z kafka-service 9092; do echo waiting for kafka; sleep 2; done;']

---
apiVersion: v1
kind: Service
metadata:
  name: branch-service
  namespace: banking
spec:
  type: NodePort
  selector:
    app: branch-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
      nodePort: 30083