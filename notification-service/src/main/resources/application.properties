spring.application.name=notification-service
server.port=8085

# PostgreSQL - For storing notification audit trail
spring.datasource.url=jdbc:postgresql://localhost:5432/notification_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=notification-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false

# Email Configuration (Using Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=neilherbertagtuca@gmail.com
spring.mail.password=F1R3F0x@374111
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# SMS Configuration
sms.provider=mock
# For Twilio:
# sms.provider=twilio
# sms.api.key=YOUR_TWILIO_ACCOUNT_SID
# sms.api.url=https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID
# sms.sender.name=+1234567890

# For Semaphore (Philippines):
# sms.provider=semaphore
# sms.api.key=YOUR_SEMAPHORE_API_KEY
# sms.api.url=https://api.semaphore.co/api/v4/messages
# sms.sender.name=YourBank

# Disable Eureka
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
spring.cloud.discovery.enabled=false

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.probes.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true